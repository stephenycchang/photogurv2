<p><%= @item[:sku] %>, <%= @item[:description] %></p>
<%= image_tag @item.url %>

<%= link_to "Back to Items", items_url %> || <%= link_to "Edit", edit_item_path(@item)  %> || <%= link_to "Delete", @item, method: :delete, data: {confirm: "Are you sure you want to delete this picture? There's no going back!"}  %>

